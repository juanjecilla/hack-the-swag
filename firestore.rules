rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public community bingo board: anyone can read and add a fun fact,
    // but existing entries cannot be edited or deleted.
    match /entries/{id} {
      allow read, create: if true;
      allow update, delete: if false;
    }
  }
}
